# railway.toml del servicio MT5
# Este archivo va dentro de mt5_service/ y Railway lo detecta
# cuando configuras el servicio apuntando a este subdirectorio.
[deploy]
startCommand = "/app/start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[build]
dockerfilePath = "Dockerfile"
