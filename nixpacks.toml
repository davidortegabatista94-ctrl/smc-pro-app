[phases.install]
cmds = [
    "python3 -m pip install --break-system-packages -r requirements.txt",
    "python3 -m textblob.download_corpora lite"
]

[start]
cmd = "streamlit run smc_pro_app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
